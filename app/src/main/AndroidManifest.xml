<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.modulo.proyectobd">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.Guardado_de_datos"></activity>
        <activity android:name=".ui.Ubicaciones_Activity" />
        <activity android:name=".Geolocalizacion.LocalizacionActivity" />
        <activity android:name=".settings.MySettings" />
        <activity android:name=".ui.MotoEspecificaActivity" />
        <activity android:name=".ui.ListaMotosActivity" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>